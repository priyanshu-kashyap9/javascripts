// in the leacture we read string in the js
const name = " priyanshu "
const repo =  5
console.log(" hello " + name + repo );

// we use new method to use the same work but it understandebale in code 

console.log(`${"hello"} i am ${name} kashyap .In github reposetry is ${repo}`);

// some other example of the string in the use in js 
 
const gameName = new String('hellom u');
console.log(gameName[0]);
console.log(gameName.length);
console.log(gameName.toUpperCase());
console.log(gameName.charAt(5));
console.log(gameName.indexOf("l"));



